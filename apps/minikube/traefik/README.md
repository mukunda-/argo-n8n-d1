## Traefik

This is a gateway controller that allows traffic to flow into the service. This is for local dev only. For AWS, the traffic is routed through an ALB with the AWS Load Balancer Controller.
