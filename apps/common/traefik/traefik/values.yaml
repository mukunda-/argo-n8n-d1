ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.n8n.local.bitovi-ai.com`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All
