architecture: standalone
auth:
  enabled: false
master:
  persistence:
    enabled: true
    size: 2Gi
image:
  repository: bitnamilegacy/redis
  tag: 7.2.4-debian-12-r9
global:
  security:
    allowInsecureImages: true
    # todo: maybe we should look for alternatives to bitnami/redis if
    # it is no longer supported for free